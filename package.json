{
  "name": "cp-vertex-gym-finder",
  "version": "1.0.0",
  "description": "CP Vertex Gym Problem Finder — Find unsolved Codeforces problems across multiple handles",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n \"vite,server\" -c \"cyan,magenta\" \"vite\" \"node server.js\"",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite": "^6.0.0"
  }
}
